import { pods } from "../index";

describe("config", () => {
  test.todo("returns PublishConfig");
});

describe("plant", () => {
  describe("unpublished note", () => {
    test.todo("creates empty note first");
  });

  describe("published note", () => {
    test.todo("publishes note and updates frontmatter");
  });

  describe("note with images in assets folder", () => {
    test.todo("uploads referenced files as attachments");
  });
});
